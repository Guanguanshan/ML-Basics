# 第 5 章 · 神经网络（周志华《机器学习》）

本目录对应教材 **第 5 章** 相关实验；当前以 **NumPy + Matplotlib** 实现，与第 3 章风格一致。

---

## 文件一览

| 文件 | 复现的模型 / 内容 | 做了什么实验 | 特点 | 产出图片 | 图片含义 |
|------|-------------------|--------------|------|----------|----------|
| `ch5_2.py` | **Sigmoid 激活的「软」单层感知机**（BCE + 全批梯度下降）与 **单隐层 MLP**（手写反向传播） | **OR（线性可分）** vs **XOR（对单层线性不可分）**；再对比 **XOR 下单层** vs **XOR 下 2→2→1 双层** | OR 用随机初值迭代；XOR 单层用**闭式最优线性参数**展示 75% 上界（BCE-GD 在 XOR 上易鞍点 50%，故不作单层迭代）；MLP 全 Sigmoid 学 XOR | `ch5_2_or_vs_xor_single.png`、`ch5_2_xor_1layer_vs_2layer.png` | 第一张：同一单层结构在 **OR 与 XOR** 上的 **0.5 概率等高线** 与着色决策区；第二张：**XOR 下单直线分界** vs **非线性隐层** 后对角两类被分开 |

---

## 依赖

见仓库根目录 `requirements.txt`（`numpy`、`matplotlib`）。
